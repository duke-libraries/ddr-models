default: &default
  adapter: postgresql
  encoding: UTF8
  pool: 5
  timeout: 5000
  username: hydra

development:
  <<: *default
  database: ddr_models_valkyrie_develop

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *default
  database: ddr_models_valkyrie_test

production:
  <<: *default
  database: ddr_models_valkyrie_prod
